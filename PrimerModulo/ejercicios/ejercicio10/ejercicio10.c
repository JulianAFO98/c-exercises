//. Reescribir cada una de las siguientes porciones de código en una sola sentencia mediante el uso
//del operador condicional:

#include <stdio.h>


main() {
    int a=1,b=2,c=3;

    /*if(a < b) {
        c = a;
    } else {
        c = b;
    }*/

    //c = (a<b) ? a : b;

    //printf("%d",c);


    /*if(a < b && b < c) {
        printf("ordenados");
    } else {
        printf("desordenados");
    }
    */
    //( a<b && b<c ? "ordenados":"desordenados");


    return 0;
}
