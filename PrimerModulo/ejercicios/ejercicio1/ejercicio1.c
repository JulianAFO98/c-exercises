#include <stdio.h>


main() {
    /*Dados los siguientes tipos de datos del lenguaje Pascal: char, integer, longint, word, real y
     double; obtener el tipo equivalente en el lenguaje C especificando la cantidad de bytes que
     ocupa y su cadena de formato. */
     //PASCAL => C

     //char = char
     //longint = long int
     //word = unsigned (short/"") int
     //real = float
     //double = double

     unsigned int b = 100000;
     printf("%d",b);

}
