//a
#include <stdio.h>
/*
int main() {
    int a, b, c = 3, d = 2;
    a = 8 - 5 * 3 + 2; //-5
    b = 7 % 3 + 4 * 2; //  1 + 8 = 9
    printf(" valor de a %d\tvalor de b %d\n", a,b); // deberia imprimir  -5  9
    b %= a; // 4
    printf(" valor de b %d\t\n", b); //deberia imprimir 4
    b =(-4) % 3; // -1
    printf(" valor de b %d\t\n", b); //deberia imprimir -1
    if(d = a) // d=2 a=-5 => un solo =? => Asigna en vez de comparar?
        printf(" d y a son iguales ");
    else
        printf(" d y a son distintos ");
    return 0;
}
*/
//b)

/*
int main() {
    int c=3, d=2;
    c -= d++ * 3; // c = c - (d++*3) => c = c - 6 => c = -3  d = 3
    printf(" valor de c %d\tvalor de d %d\n", c, d);
    return 0;
}
*/
//c)
/*
int main() {
    int c = 3, l; //l = ?
    c = (c * 2 - (l = 4, -- l)); // c= 6-3 = 3  l=3
    printf(" valor de c %d\t valor de l %d\n", c, l);
    return 0;
}
*/
